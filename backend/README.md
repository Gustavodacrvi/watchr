# backend-only-webpack-preset
Fast backend preset with webpack, typescript, lint and live loading with nodemon.

## Initialize server on port 3000
```
npm run serve
```

### Build for production
```
npm run build
```
